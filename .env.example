# =================
# MATTERMOST CONFIG
# =================
# URL of your Mattermost server (e.g., https://mattermost.example.com)
MATTERMOST_URL=https://your-mattermost-server.com

# Mattermost bot token (created in System Console > Integrations > Bot Accounts)
MATTERMOST_TOKEN=your_mattermost_bot_token

# Mattermost team name where the bot will be active
MATTERMOST_TEAM_NAME=your_team_name

# Channels to listen to (regex supported, comma-separated)
# Examples: "dev-.*,bugs,support" listens to all dev-* channels, bugs and support
MATTERMOST_CHANNELS=dev-.*,bugs,support

# Bot name for mentions (@deputy by default)
MATTERMOST_BOT_NAME=deputy

# ============
# LLM CONFIG
# ============
# LLM provider: "openai" or "anthropic"
LLM_PROVIDER=openai

# Model to use
LLM_MODEL=gpt-4o-mini

# OpenAI API key (if LLM_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic API key (if LLM_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# LLM temperature (0.0 to 1.0, default: 0.1)
LLM_TEMPERATURE=0.1

# Maximum number of tokens for response (default: 2000)
LLM_MAX_TOKENS=2000

# ==============
# GITHUB CONFIG
# ==============
# GitHub token with repo permissions (Settings > Developer settings > Personal access tokens)
GITHUB_TOKEN=your_github_token_here

# GitHub organization or username
GITHUB_ORG=your_github_org

# Repository name to create issues in
GITHUB_REPO=your_github_repo

# =======================
# ISSUE CREATION CONFIG
# =======================
# Automatic labels added to each issue (comma-separated)
ISSUE_AUTO_LABELS=auto-generated,deputy-bot

# Default assignee for new issues
ISSUE_ASSIGNEE=your_github_username

# GitHub project ID (optional)
ISSUE_PROJECT_ID=

# =============
# SENTRY CONFIG
# =============
# Sentry DSN (found in Settings > Projects > [Project] > Client Keys)
SENTRY_DSN=https://your_key@sentry.io/your_project_id

# Sentry organization
SENTRY_ORG=your_sentry_org

# Sentry project name
SENTRY_PROJECT=your_sentry_project

# Sentry authentication token (Settings > Account > API > Auth Tokens)
SENTRY_AUTH_TOKEN=your_sentry_auth_token

# Default period for Sentry commands (24h or 7d only)
SENTRY_DEFAULT_PERIOD=24h

# =============
# APP CONFIG
# =============
# Debug mode (true/false)
DEBUG=true